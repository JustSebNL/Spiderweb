# UI Assets

This directory holds project-owned UI surfaces and reusable visual assets.

## Current Contents
- `backgrounds/`: approved dashboard background assets
- `dashboard/`: interactive dashboard and operator UI prototypes that are intended to evolve into real product surfaces

## Split
- `ui/`: canonical reusable UI files that the project can reference
- `dev/samples/`: inspiration, experiments, and non-canonical visual references
