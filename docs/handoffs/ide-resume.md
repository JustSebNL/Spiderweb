# IDE Resume Handoff

This file exists so a new IDE-backed session can continue from local project state instead of relying on prior chat context.

Open `dev/task-handoff.md` first if you are carrying a local handoff file.
